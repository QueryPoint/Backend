services:
  backend:
    build: .
    container_name: querypoint_backend
    restart: unless-stopped
    ports:
      - "8000:8000"
    volumes:
      - ./config.toml:/app/config.toml:ro
    networks:
      - querypoint

networks:
  querypoint:
    external: true
    name: querypoint_network
